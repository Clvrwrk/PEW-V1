---
import BaseLayout from '../../components/seo/BaseLayout.astro';
import HeroSection from '../../components/molecules/HeroSection.astro';
import Breadcrumb from '../../components/molecules/Breadcrumb.astro';
import LeadCaptureForm from '../../components/molecules/LeadCaptureForm.astro';
import { breadcrumbSchema } from '../../lib/schema/BreadcrumbList';

const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Property Card', href: '/property-card/' },
  { label: 'Get Yours', href: '/property-card/get-yours/' },
];
const schemaPayload = [breadcrumbSchema(breadcrumbs)];
---

<BaseLayout
  title="Request Your Pro Exteriors Property Card"
  description="Enter your address and we'll prepare your free Pro Exteriors Property Card — a complete health and history report for your home's exterior."
  canonicalPath="/property-card/get-yours/"
  jsonLd={schemaPayload}
>
  <Breadcrumb items={breadcrumbs.slice(1)} />
  <HeroSection
    image="/images/home-hero.webp" title="Request Your Property Card" subtitle="Enter your address. We'll prepare it within 48 hours." />
  <section class="py-16 px-4 max-w-2xl mx-auto">
    <h2 class="text-3xl font-bold mb-2 text-white">Request your Property Card</h2>
    <p class="text-text-secondary mb-6">Enter your details and property address — we'll prepare your free exterior health and history report within 48 hours.</p>
    <LeadCaptureForm
      ctaId="property-card"
      source="Website – Property Card Request"
      intent="lead"
      propertyType="Residential"
      serviceType="Inspection"
      submitLabel="Request my Property Card"
    />
  </section>
</BaseLayout>
