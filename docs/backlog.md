# Pending Backlog — Pro Exteriors Website

> Persistent backlog of deferred work. Survives handoff rotation (unlike the
> "Blockers / Awaiting Human" list inside `docs/handoffs/current.md`, which
> archives each session). Add an entry here when work is intentionally deferred;
> remove it when shipped. Cross-reference the handoff for in-flight detail.

**Maintainer:** Maren Castellan-Reyes · **Last updated:** 2026-05-29

---

## Content / E-E-A-T (blocked on client-specific material)

- [ ] **Develop service-page unique content** — Re-enable the pass-through links on
  the residential services grid (`src/pages/residential-roofing/index.astro`,
  `serviceGrid` → `card.href`/`card.anchor`) only after each linked service page
  carries genuinely unique, client-specific content. Linking them while they're
  thin invites Google's "Crawled — currently not indexed" status and dilutes
  E-E-A-T. Same caution applies to the `RoofingContractor` schema
  `hasOfferCatalog` URLs on that page and to the commercial equivalents.
  _Deferred 2026-05-29._

- [ ] **Develop Residential Case Studies** — Re-add the residential case-studies
  section (markup removed 2026-05-29; `caseStudies` array still defined in the
  page frontmatter) once the client supplies real, signed-off completed projects
  (TX / CO / KS or substitute) with permission to publish. No [REPRESENTATIVE]
  case studies ship — CLAUDE.md trust gate. _Deferred 2026-05-29._

- [ ] **Build location pages** — Per-office location pages
  (`/locations/{office}/`, generated by `src/pages/locations/[office]/index.astro`
  from the `offices` content collection) need unique, GBP-aligned content before
  we surface them. "View Office Details" links removed from the locations grid
  2026-05-29; re-add `loc.url` links once the pages ship with real content.
  Note: the locations `CollectionPage` schema still references these URLs — align
  that with the page rollout. _Deferred 2026-05-29._

---

## Forms / GoHighLevel (in progress — see handoff)

- [ ] **Dynamic popup CTA forms** — Reusable modal form/survey wired to all primary
  CTAs. Min fields name/email/phone; ideal first/last/email/phone/address/note;
  multi-step survey mode when >4 questions. DESIGN.md-compliant. (Build in
  progress — see handoff.)

- [ ] **GoHighLevel intake wiring** — Connect forms to the four-lane GHL intake
  (contact upsert → dedupe → routed opportunity → tags) per the webdev guide.
  **Blocked on a server-side decision:** the site is `output: 'static'`, so the
  Private Integration Token cannot live in the build — a server-side proxy
  (serverless function, Make.com scenario, or GHL inbound webhook) must hold it.
  Awaiting Chris's call on where that proxy runs.

---

## Carried from handoff (launch gates / client assets)

- [ ] Official BBB seal + Chamber badge images (footer `data-todo`).
- [ ] Lead-form destinations — Madison (production) / Crystal (service) + delivery
  method; end-to-end test before launch.
- [ ] Real metrics + Atlanta address (pending from client).
- [ ] Launch hard gates — Lighthouse, a11y, schema, live form tests.
- [ ] Real Pro Exteriors photos to replace the six AI service-card placeholders.
